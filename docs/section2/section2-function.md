# Section 2: Function type


### Function
- Only Function keyword is not enough, more function details are needed.
- Function detail definition:
  ```
    () => number
    (a: number, b: number) => number;
  ```
